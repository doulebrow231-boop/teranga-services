import React from "react";
import { createRoot } from "react-dom/client";
import TerangaApp from "./TerangaApp.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TerangaApp />
  </React.StrictMode>
);
